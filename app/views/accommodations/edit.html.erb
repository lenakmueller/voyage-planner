<div class="form-accommodation">
  <%= link_to trip_path(@trip), class: "d-flex justify-content-start text-decoration-none" do %>
    <i class="fa-solid fa-arrow-left" id="back_button"></i>
  <% end %>
  <h1 class="text-center fs-1 py-4" id="h1-color">Edit your stay</h1>

  <%= render "form_acc", accommodation: @accommodation, trip: @trip %>
</div>
